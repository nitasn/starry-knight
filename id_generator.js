'use strict';

/*
// this works, but it's not needed

const new_unique_id = (() => {

    let last_result = 0;

    return function creator() {
        last_result++;
        return last_result;
    }
})();

//*/